# RLLC – Anonymous Review Demo

This repository provides an **anonymized demo implementation** for the manuscript submission.
It is intended **only for reviewer verification and reproducibility checks**.

## Overview
The code contains:
- the main entry script: `main.py`
- model implementations under `model/`
- (optional) saved outputs under `best_save/` (may be empty)
- local dataset cache under `data/` (not required if you download data during execution)

## Environment
- All required dependencies are listed in **`requirements.txt`**.

## Installation
Create a clean environment (recommended) and install dependencies:
```bash
pip install -r requirements.txt
